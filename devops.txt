<لاؤرلارؤلاؤر];

    $stmt = $conn->prepare("DELETE FROM codes WHERE id = ?"
    $stmt->bind_param("i", $code_id);ؤلالاؤر

    if ($stmt->execute()) {
        echo "Code deleted successfully";
    } else {
        echo "Error deleting code: " . $conn->error;
    }

    $stmt->close(;
}

